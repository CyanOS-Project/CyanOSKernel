dist: focal
language: cpp
compiler:
- clang++

addons:
  apt:
    update: true
    packages:
    - nasm
    - grub2
    - xorriso
    - cmake
    - mtools

script:
- mkdir build && cd build
- cmake ..
- make
- make test